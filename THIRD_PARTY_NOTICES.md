# Third-Party Notices

AimTune AI bundles local MediaPipe Tasks Vision runtime files and the EfficientDet Lite object detector model so gameplay analysis can run offline without runtime CDN downloads.

- `@mediapipe/tasks-vision`: Apache-2.0, Google.
- `public/models/efficientdet_lite0_uint8.tflite`: EfficientDet Lite object detector model distributed for MediaPipe Tasks.

AimTune uses these assets only for cautious local candidate target signals. The app does not claim game-specific enemy detection in v1.
